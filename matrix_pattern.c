#include <stdio.h>
#include <stdlib.h>

struct Arr {
    int *arr;
    int dimension;
}

int main(){
    int n;
    scanf("%d",&n);
    struct Arr array;
    while(n--){
        scanf("%d",&array.dimension)
    }
    return 0;
}